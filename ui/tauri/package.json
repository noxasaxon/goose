{
  "name": "goose-tauri",
  "version": "1.0.30",
  "private": true,
  "scripts": {
    "prepare-sidecar": "node scripts/prepare-sidecar.js",
    "dev": "npm run prepare-sidecar && cargo tauri dev",
    "build": "npm run prepare-sidecar && cargo tauri build",
    "tauri": "cargo tauri"
  }
}